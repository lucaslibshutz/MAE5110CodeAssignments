[project]
name = "mae5110"
version = "0.1.0"
description = "Course exercises for MAE 5110"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "ipython>=9.16.1",
    "matplotlib",
    "numpy",
]

[dependency-groups]
dev = [
    "pytest",
]
